services:
  - type: web
    name: dsmentor
    runtime: python
    buildCommand: pip install -r requirements-deploy.txt && python data/generate_dataset_simple.py
    startCommand: uvicorn services.api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.5"
